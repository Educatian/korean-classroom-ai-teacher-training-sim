{
  "name": "teacher-training-research-collector",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run typecheck && npm run lint && npm test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "lint": "biome check src tests",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.5",
    "ky": "^1.10.0",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@cloudflare/workers-types": "^5.20260719.1",
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.93.0"
  }
}
